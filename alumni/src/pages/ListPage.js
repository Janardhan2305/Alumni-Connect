const ListPage = ({ title, data }) => { if (!data || data.length === 0) return (<div><h2 className="text-3xl font-bold text-gray-800 mb-8">{title}</h2><div className="bg-white p-8 rounded-xl shadow-lg"><p className="text-gray-600">No data available.</p></div></div>); const headers = Object.keys(data[0]).filter(key => key !== 'id' && key !== 'createdAt'); return ( <div> <h2 className="text-3xl font-bold text-gray-800 mb-8">{title}</h2> <div className="bg-white p-8 rounded-xl shadow-lg overflow-x-auto"> <table className="min-w-full divide-y divide-gray-200"> <thead className="bg-gray-50"><tr>{headers.map(header => (<th key={header} scope="col" className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{header}</th>))}</tr></thead> <tbody className="bg-white divide-y divide-gray-200">{data.map(item => (<tr key={item.id}>{headers.map(header => (<td key={header} className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{item[header]}</td>))}</tr>))}</tbody> </table> </div> </div> ); };


export default ListPage;